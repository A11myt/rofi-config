# Rofi Configuration

This directory contains my complete Rofi configuration for Linux. All settings, color schemes, and layouts are consolidated in the `config.rasi` file.

## File

- **config.rasi** – Central configuration file for appearance, colors, and behavior of Rofi.

## Installation

1. Copy `config.rasi` to `~/.config/rofi`.
2. Make sure Rofi is installed:

   ```bash
   sudo apt install rofi
